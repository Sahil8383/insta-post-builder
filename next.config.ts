import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* If you see a Turbopack "multiple lockfiles" warning, set turbopack.root to this
   * app's directory or remove stray package-lock.json files outside the project. */
};

export default nextConfig;
